<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.HMusic" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowActivityTransitions">true</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">#FFFFFF</item>
        <item name="android:windowLightStatusBar">@bool/statusBarForeground</item>
        <item name="android:statusBarColor">@color/colorTransparency</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowBackground">@color/colorPageBackground</item>

        <!-- v29 禁用系统黑暗模式 -->
        <item name="android:forceDarkAllowed">false</item>

        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>

    </style>

    <!-- 透明全屏 -->
    <style name="Theme.HMusic.FullScreen.Translucent">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowBackground">@color/colorTransparency</item>-->
    </style>

    <!-- 全屏 -->
    <style name="Theme.HMusic.FullScreen">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>
</resources>